package strings;

public class Strings {

	public static void main(String[] args) {
		/* String gg = new String("hello hy");
		 
		gg.equals(str)        == it will compare the 2 strings
		gg.equalIgnoreCase(str) == it compre the strings by neglecting the case 
		gg.length          == gives the length of the string
		indexOf(gg)        == gives the index value of first character in given string
		gg.charAt(index)   == gives the character present in the given index value
		gg.replace(old String,new String) == replace the old string with new string
		gg.toLowerCase     == everything will be converted to lower case
		gg.toUpperCase     == everything will be converted to uppercase
		gg.compareTo(str)  == here it compare the two strings then it will print the follows
		                           it will zero if strings are equal
		("here comparison 
		is based on unicodes      +ve == if  first mismatch of first string > first mismatch of second string 
		ascii values")             -ve == if  first mismatch of first string < first mismatch of second string 
		
		gg.trim()          == remove all the white spaces appear before&after the  string
		gg.concat(str)    == it will concatnate the string
		
		  */

	}

}
